#include <stdio.h>

int main()
{
   char str[50];

   char x;
	int i;

   printf("Enter a string : ");

	scanf("%d", &i);
	x = (char) i;

   //gets(str);
   char c = 'z';
   printf("c =: %c\n", x);

   printf("c =: %d", x);

   return(0);
}